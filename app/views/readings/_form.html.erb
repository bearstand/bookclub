<div class="bookclub_form">

<%= form_for(@reading) do |f| %>
  <% if @reading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reading.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @reading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <legend><%= @reading.book.title %></legend>

  <div class="field">
    <%= f.label :rating %>
    <%= f.select :rating, Reading::RATING_NUMBERS %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
 <div class="actions">
    <%= f.submit %>
  </div>

 </fieldset>
<% end %>

</div>
